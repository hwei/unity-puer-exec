# Legacy Docs Redirect

This repository now uses OpenSpec as the canonical governance and specification system.

Use these entry points instead:

- `openspec/project.md` for repository-wide context
- `openspec/specs/` for durable requirements
- `openspec/changes/` for active and archived change work

This file remains only as a transitional redirect for older sessions.
