# Legacy Plan Template Redirect

This repository no longer treats `docs/plans/` and `docs/plan-template.md` as the canonical planning system.

Use OpenSpec change artifacts in `openspec/changes/` instead.
