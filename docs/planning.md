# Legacy Planning Redirect

The repository now plans substantial work through OpenSpec changes instead of the legacy `docs/plans/` workflow.

Use:

- `openspec/project.md` for project-wide expectations
- `openspec/changes/` for proposal, design, specs, and tasks

This file remains only as a transitional redirect.
