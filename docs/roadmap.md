# Legacy Roadmap Redirect

The repository no longer uses `docs/roadmap.md` as the canonical backlog or task graph.

Use OpenSpec instead:

- durable requirements: `openspec/specs/`
- active scoped work: `openspec/changes/`
- completed change history: `openspec/changes/archive/`

This file remains only as a transitional redirect so older references fail softly.
