# Legacy Status Redirect

The legacy repository-wide status snapshot is retired.

Current work should be tracked through OpenSpec changes under `openspec/changes/`.

If a short current-state note is needed, record it in the relevant active change instead of restoring `docs/status.md` as a canonical status board.
