# Status

## Current Focus

- The formal product boundary is now defined; next work is migrating the Unity package into this repository and formalizing the CLI surface.

## In Progress

- No implementation task is currently in progress after the normalization pass completes.

## Blocked

- No active blocker is currently recorded.

## Next

- Start `T1.2` to move the Unity package into this repository and rename it away from the validation-only identity.
- Start `T1.4` to make `unity-puer-exec` the authoritative CLI entry with complete help and stable commands.
- Use `T1.5` to decide whether the CLI should remain script-based or ship as a more self-contained executable.
