# Status

## Current Focus

- Reframe the repository around a formal Unity package plus a formal CLI instead of the current migration-oriented skill layout.

## In Progress

- No implementation task is currently in progress after the normalization pass completes.

## Blocked

- No active blocker is currently recorded.

## Next

- Start `T1.1` from `docs/roadmap.md` to define the formal product boundary and naming.
- Lock `T1.1.1` so the validation host is treated as a baseline fixture with local-only test injection by default.
- Follow with `T1.2` to move the Unity package into this repository.
- Use `T1.4` and `T1.5` to turn the CLI into the authoritative product surface and decide how self-contained it must be for end users and agents.
