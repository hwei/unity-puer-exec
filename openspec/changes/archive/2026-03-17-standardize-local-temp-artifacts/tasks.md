# Tasks

## 1. Repository Policy

- [x] Add `.tmp/` to git ignore rules
- [x] Document `.tmp/` as the preferred local location for transient validation scripts and probes

## 2. Adoption

- [x] Move or recreate current local probe scripts under `.tmp/`
- [x] Verify the working tree stays clean when `.tmp/` is used
