## 1. Validation Record

- [x] 1.1 Add a repository-owned validation note for the OpenUPM real-host experiment, including host cleanup, proxy-gated acquisition, import stabilization, and the final successful `BuildBundle` workflow result.
- [x] 1.2 Record the confirmed follow-up candidates from the experiment: missing package `.meta` warning, harmless-but-noisy transport disconnect during unstable import, and repeated marker verbosity.

## 2. Durable Truth Updates

- [x] 2.1 Update `validation-host-integration` so the published OpenUPM path and its environmental blockers are part of the durable real-host validation story.
- [x] 2.2 Update `readme-agent-onboarding` so installation guidance tells the agent to ask for proxy settings when OpenUPM registry access fails.

## 3. Documentation Follow-Through

- [x] 3.1 Update `ReadMe.md` and `ReadMe.zh-CN.md` installation guidance to reflect the proxy-aware recovery path.
- [x] 3.2 Re-read the updated README prompts against the recorded experiment and confirm they would have reduced the observed agent friction.
