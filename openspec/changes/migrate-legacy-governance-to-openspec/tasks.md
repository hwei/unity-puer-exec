## 1. Capture the migration contract

- [x] 1.1 Write the migration proposal, design, and change-local specs for repository governance, validation-host integration, and formal CLI contract
- [x] 1.2 Review the existing legacy governance and decision docs to confirm which content becomes project guidance versus long-lived OpenSpec specs

## 2. Implement the OpenSpec baseline

- [x] 2.1 Create `openspec/project.md` with repository context, environment setup, and OpenSpec working agreements
- [x] 2.2 Create long-lived specs under `openspec/specs/` for repository governance, validation-host integration, and formal CLI contract
- [x] 2.3 Update `openspec/config.yaml` with repository context and artifact rules that reinforce the new workflow

## 3. Retire legacy authority

- [x] 3.1 Update `AGENTS.md` and `ReadMe.md` so they point to OpenSpec as the governance/specification entry
- [x] 3.2 Convert legacy top-level workflow docs in `docs/` into transitional redirects instead of canonical rules
- [x] 3.3 Validate OpenSpec status and leave the migration change ready for archive
