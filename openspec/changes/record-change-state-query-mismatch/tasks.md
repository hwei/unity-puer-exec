## 1. Problem Capture

- [x] 1.1 Reproduce and record the concrete mismatch between raw `meta.yaml.status`, repository-local planning views, and `openspec list --json` workflow status.
- [x] 1.2 Map the problem to the existing `change-backlog-triage` capability instead of creating a duplicate governance surface.

## 2. Exploration Outcome

- [x] 2.1 Record the preferred follow-up direction of a repository-local diagnostic query surface that exposes raw, derived, and workflow state side by side.
- [x] 2.2 Leave implementation details unresolved so a later change can analyze the actual wrapper, tooling, or hygiene approach separately.

## 3. Closeout Readiness

- [x] 3.1 Mark this spike as a problem-recording change that is ready either for archive or for supersession by a future implementation-oriented follow-up.
- [x] 3.2 Preserve the issue statement without forcing an immediate backlog-tool or workflow-tool rewrite in this change.
