## 1. Durable Governance Updates

- [x] 1.1 Update the `change-backlog-triage`, `repository-governance`, and `apply-closeout-review` delta specs to remove `queued` as normal backlog truth and document `openspec status` as artifact readiness only.
- [x] 1.2 Update the new change's proposal and design if spec drafting reveals a narrower or broader metadata migration scope.

## 2. Tooling And Guidance Migration

- [x] 2.1 Update repository guidance files and metadata conventions so they no longer describe `queued` as the backlog definition.
- [x] 2.2 Update change scaffolding and backlog tooling so newly created changes and backlog queries align with derived `eligible` state.

## 3. Validation And Closeout

- [x] 3.1 Validate the migrated tooling and guidance on representative change metadata, including legacy `queued` compatibility.
- [x] 3.2 Summarize any remaining migration debt and leave the change ready for archive or follow-up implementation.
