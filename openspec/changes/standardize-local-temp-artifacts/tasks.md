# Tasks

## 1. Repository Policy

- [ ] Add `.tmp/` to git ignore rules
- [ ] Document `.tmp/` as the preferred local location for transient validation scripts and probes

## 2. Adoption

- [ ] Move or recreate current local probe scripts under `.tmp/`
- [ ] Verify the working tree stays clean when `.tmp/` is used
